pipeline {
    agent any

    stages {
        stage('Set Up Environment') {
            steps {
                sh '''
                sudo apt-get update
                sudo apt-get install -y python3 python3-pip
                '''
            }
        }

        stage('Run tests') {
            steps {
                sh 'python3 tests.py'
            }
        }
    }
}